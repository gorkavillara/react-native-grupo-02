import { View, Text } from "react-native"
import React from "react"

const AndroidComponent = ({ text }: { text: string }) => {
    return (
        <View>
            <Text>AndroidComponent</Text>
        </View>
    )
}

export default AndroidComponent
